FROM golang:1.24-alpine

# Install air for hot reload + build tools
RUN apk add --no-cache git curl && \
    go install github.com/air-verse/air@v1.52.3

WORKDIR /app

# Copy module files first for layer caching
COPY go.mod go.sum ./
RUN go mod download

CMD ["air", "-c", ".air.toml"]
